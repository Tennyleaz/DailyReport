<Window x:Class="DailyReport.TimeSpanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DailyReport"
        mc:Ignorable="d"
        WindowStyle="ToolWindow" ShowInTaskbar="False"        
        WindowStartupLocation="CenterOwner"
        Title="TimeSpanWindow" Height="125" Width="200">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#B388FF"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="BorderThickness" Value="0"></Setter>
        </Style>
    </Window.Resources>
    
    <StackPanel>
        <DatePicker Name="dateStart"/>
        <DatePicker Name="dateEnd"/>
        <CheckBox Name="mergeProjectBox" Content="Merge Same Project" HorizontalAlignment="Center" Height="17"/>
        <Button Name="btnOK" Click="btnOK_Click" Height="20">OK</Button>
    </StackPanel>
</Window>
